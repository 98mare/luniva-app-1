import { InsertupdateCollectorLocationDetails, GetlocationofCollectorByDate} from '../constants/url'
import { fetch, store } from '../utils/httpUtil'

export const InsertCollectorDetails = (data, sucessCallback) => {
  return async dispatch => {
    try{
      
    }
  }
}